---
title: Статус
description: Статус проектов Opensophy.
---

<script lang="ts">
	import SiteMenu from '$lib/components/ui/SiteMenu.svelte';
	import Badge from '$lib/components/ui-registry/Badge.svelte';
	import Button from '$lib/components/ui-registry/Button.svelte';
	import Progress from '$lib/components/ui-registry/Progress.svelte';

	const projects = [
		{
			name: 'os.docs',
			badges: [{ label: 'релиз', variant: 'success' }],
			github: 'https://github.com/opensophy-projects/docs',
			progress: 100
		},
		{
			name: 'os.mtls',
			badges: [
				{ label: 'в разработке', variant: 'warning' },
				{ label: 'новое обновление', variant: 'accent' }
			],
			github: 'https://github.com/opensophy-projects/mtls',
			progress: 60
		},
		{
			name: 'os.ui',
			badges: [
				{ label: 'релиз', variant: 'success' },
				{ label: 'пополняется контентом', variant: 'accent' }
			],
			github: 'https://github.com/opensophy-projects/site/tree/main/apps/web/src/lib/components/ui-registry',
			progress: 100
		},
		{
			name: 'os.port',
			badges: [{ label: 'в разработке', variant: 'warning' }],
			github: null,
			progress: 50
		},
		{
			name: 'os.net',
			badges: [{ label: 'в заморозке', variant: 'outline' }],
			github: null,
			progress: 50
		}
	];
</script>

<main class="min-h-dvh bg-background text-foreground">
	<SiteMenu />

	<div class="mx-auto flex w-full max-w-5xl flex-col items-center gap-8 px-4 py-24 md:py-32">
		<div class="space-y-3 text-center">
			<p class="text-sm font-medium uppercase tracking-[0.18em] text-foreground-muted">opensophy</p>
			<h1 class="text-4xl font-medium tracking-tight text-foreground md:text-6xl">Статус проектов</h1>
		</div>

		<div class="w-full max-w-4xl overflow-x-auto rounded-2xl border border-border">
			<table class="w-full border-collapse text-left">
				<thead>
					<tr class="border-b border-border">
						<th class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-foreground">
							Название проекта
						</th>
						<th class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-foreground">
							Статус
						</th>
						<th class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-foreground">
							GitHub
						</th>
						<th class="whitespace-nowrap px-6 py-4 text-sm font-semibold text-foreground">
							Прогресс
						</th>
					</tr>
				</thead>
				<tbody>
					{#each projects as project, i}
						<tr class={i < projects.length - 1 ? 'border-b border-border' : ''}>
							<td class="whitespace-nowrap px-6 py-4 font-semibold text-foreground">
								{project.name}
							</td>
							<td class="px-6 py-4">
								<div class="flex flex-wrap gap-1">
									{#each project.badges as badge}
										<Badge variant={badge.variant}>{badge.label}</Badge>
									{/each}
								</div>
							</td>
							<td class="px-6 py-4">
								{#if project.github}
									<Button href={project.github} variant="outline" size="sm">GitHub</Button>
								{:else}
									<span class="text-foreground-muted">—</span>
								{/if}
							</td>
							<td class="px-6 py-4">
								<div class="flex min-w-36 items-center gap-3">
									<Progress progress={project.progress} class="min-w-24" />
									<span class="text-sm text-foreground-muted">{project.progress}%</span>
								</div>
							</td>
						</tr>
					{/each}
				</tbody>
			</table>
		</div>
	</div>
</main>