---
title: MDSVEX Structure Components
name: Structure Components
description: Reference samples for table and step-based components, including InstallationTabs.
category: Reference
---

<script lang="ts">
	import { InstallationTabs, Step, Steps } from '$lib';
	import { ComponentPreview } from '$lib';
	import demoSource from "../../components/ui/ThemeToggle.svelte?raw";
	import ThemeToggle from "../../components/ui/ThemeToggle.svelte";
	import demoSource2 from "../../stores/theme.svelte.ts?raw";
</script>

This page covers the remaining structural and utility components used in mdsvex content.

## InstallationTabs component

<InstallationTabs pkg="@motion-core/example" />

## Table sample

| Component | Source syntax                  | Notes                                    |
| --------- | ------------------------------ | ---------------------------------------- |
| `table`   | Pipe table markdown            | Wraps `thead`, `tbody`, `tr`, `th`, `td` |
| `Steps`   | `<Steps>...</Steps>`           | Step container with counter context      |
| `Step`    | `<Step title="...">...</Step>` | One numbered row; `title` is optional    |

## Steps and Step sample

<Steps>
	<Step title="Step with title">
		Reference text only. No tutorial sequence, just structure and spacing.
	</Step>

    <Step title="Second step">
    	Another sample row showing numbering and title alignment.
    </Step>

    <Step>
    	Sample row without a `title` prop.
    </Step>

</Steps>

## Component Preview

<ComponentPreview
sources={[
{ name: "ThemeToggle.svelte", code: demoSource, language: "svelte" },
{ name: "theme.svelte.ts", code: demoSource2, language: "typescript" },
]}>

    <ThemeToggle />

</ComponentPreview>
