<script>
  export let size = 16;

  export let title = undefined;

  $: labelled = $$props["aria-label"] || $$props["aria-labelledby"] || title;
  $: attributes = {
    "aria-hidden": labelled ? undefined : true,
    role: labelled ? "img" : undefined,
    focusable: Number($$props["tabindex"]) === 0 ? true : undefined
  };
</script>

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 32 32"
  fill="currentColor"
  preserveAspectRatio="xMidYMid meet"
  width={size}
  height={size}
  {...attributes}
  {...$$restProps}>
  {#if title}<title>{title}</title>{/if}
  <path stroke-width="0" d="M2 3H10V5H2z"></path><path stroke-width="0" d="M6 9H30V11H6z"></path><path stroke-width="0" d="M6 15H30V17H6z"></path><path stroke-width="0" d="M2 27H10V29H2z"></path><path stroke-width="0" d="M6 21H22V23H6z"></path>
</svg>